<%= image_tag(item.image, class: 'default_image') %>
<%= link_to(item.title, item) %>
<%= item.description %>
<% if current_user.admin? %>
  <%= link_to 'Edit', edit_admin_item_path(item) %>
<% end %>
<%= item.price %>