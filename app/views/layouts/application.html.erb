<!DOCTYPE html>
<html>
<head>
  <title>Cowboy Kyle's Farmers Market</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
  <link href='//fonts.googleapis.com/css?family=Cookie' rel='stylesheet' type='text/css'>
  <link href='//fonts.googleapis.com/css?family=Pacifico' rel='stylesheet' type='text/css'>
  <link href='//fonts.googleapis.com/css?family=Lobster' rel='stylesheet' type='text/css'>
  <%= favicon_link_tag "hotdog.ico" %>
</head>
<body>
<div id="wrap">
  <div class="nav">
  <% if current_user.admin? %>
    <%= render partial: "layouts/admin_navigation" %>
  <% else %>
    <%= render partial: "layouts/navigation" %>
  <% end %>
  </div>

  <div class="container-fluid">
    <div class="flash">
      <div class="row">
        <div class="col-md-4 col-md-offset-4 text-center">
          <% flash.each do |type, message| %>
          <%= content_tag :div, message, id: "flash_#{type}" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>

  <div class="container-fluid">
    <%= yield %>
  </div>
</div>

  <div id="footer">
      <div class="container"><br><br>
        <p class="footer_text"><em>We at Cowboy Kyle's Farmers Market</em></p>
      </div>
  </div>

</body>
</html>
